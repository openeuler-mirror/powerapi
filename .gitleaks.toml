# Gitleaks 配置：保留默认规则集，后续按需追加 allowlist/自定义规则
[extend]
useDefault = true

# 仅当需要 allowlist/自定义规则时取消注释并按需调整：
# [allowlist]
# description = "测试与文档中的示例密钥"
# regexes = [
#   '''EXAMPLE[A-Z0-9_]+''',
# ]
# paths = [
#   "tests/",
#   "docs/",
# ]
